import React from 'react'
import { useAuth } from '../../context/AuthContext'

function Home() {
  // const {user} = useAuth()
  // console.log(user);
  
  return (
    <div>admni Home</div>
  )
}

export default Home